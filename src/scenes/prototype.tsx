import {useState} from 'react';import {createRoot} from 'react-dom/client';import {Viewer} from './Viewer';
function Prototype(){const [e,setE]=useState(0);const [orbit,setOrbit]=useState({azimuth:0,elevation:.34,zoom:1});return <main style={{height:'100vh',display:'grid',gridTemplateRows:'1fr 60px'}}><Viewer drinkId="negroni-express" expansion={e} onExpansionChange={setE} orbit={orbit} onOrbitChange={setOrbit}/><div style={{display:'flex',gap:20,padding:15,color:'#fff'}}><button onClick={()=>setE(e>.5?0:1)}>{e>.5?'Reassemble':'Explore ingredients'}</button><input aria-label="Expansion" type="range" min="0" max="1" step=".01" value={e} onChange={ev=>setE(Number(ev.target.value))}/><span>Live GLB · posters disabled · optical prototype</span></div></main>}createRoot(document.getElementById('root')!).render(<Prototype/>);
