# Behind the Bar evidence

Hardware measurements identify the owner's actual Mac16,8 Apple M4 Pro, macOS 26.3, installed Chrome 153. Synthetic transport fixtures and emulated touch input are not physical-device acceptance evidence. Current deployment and gate states are in ../RELEASE.md and ../STATUS.md.

## Public application

- `production-deployment.json`: sanitized Vercel readiness, deployment ID, exact integrated application revision, public aliases and entry asset.
- `production-assets.json`: 32 public routes/assets, status/content types and static asset SHA-256 comparison with the release checkout.
- `production-suite.json`: final public studio results; inspect its timestamp and release manifest for revision attribution.
- `preparation-production.webm`: complete three-vessel preparation, partial transfer, oil/garnish choice, comparison and return to the catalogue.
- `baseline-studio-serving.png` / `baseline-reference-comparison.png`: legal baseline transfer and reference inspection. Dynamic liquid, original glass and green pepper, clear serving ice; baseline remains labelled as demo origin.
- `keyboard-variant-0.png`, `keyboard-variant-1.png`, `serving-personal-desktop.png`: distinct actual fill/garnish results retained through service, never replaced with the canonical assembly.
- `preview-390x844.png`, `preview-768x1024.png`, `touch-serving-reduced-motion.png`: responsive and emulated-touch evidence. The filenames are historical; latest public suite run provenance is in the release manifest. No phone motion control ships.
- `release-performance.json` / `release-performance-scene.png`: 30-second normal headful Chrome measurement, raw frame intervals, actual GPU adapter/dispatches and separate 30FPS floor / 60FPS target.

## Native evidence and limits

- `native-preflight-stream.ndjson.gz` / `native-preflight-summary.json`: 300 seconds of real accel/gyro, approximately 100.62 Hz each, no stall; no confirmed physical-phase labels.
- `production-loopback-probe.json`: normal Chrome HTTPS→authenticated loopback status, with sanitized startup arguments and no security/focus/permission override.
- `bridge-protocol.json`: running bridge rejects bad authorization, untrusted Origin and duplicate subscriber.
- `production-native-recovery.json`: actual reader pause/resume, real browser visibility change, neutral no-pour/source disarm, helper restart. Corrected release adds a real-report coalescing test via a deliberate 2.5-second diagnostic-page stall.
- `production-native-session.json`, `production-native-summary.json`, `native-video/production-native.webm`: first public 772c3e2 native session. Actual HID reports, but motion disarmed after 6.3 seconds and the later pour was local. Coalesced-delivery disconnection at 69.7seconds motivated the parser correction. Do not claim physical tilt-pour or uninterrupted success from this capture.
- `production-manual-attempt.json`: 120-second manual prompt timed out without arming; no physical action evidence captured.
- `buffering-fix-preview-recovery.json`, `buffering-fix-preview-suite.json`: corrected receiver's actual-sensor recovery and 20 studio checks. Deterministic buffered/stale/malformed stream cases are explicitly fixtures.

Screen video does not show the start of external laptop movement. Sample-timestamp→frame is a synchronized same-host estimate, separate from browser-receipt→frame; neither proves undocumented sensor-internal latency or physical onset→photons. BTB-06 and that part of BTB-10 remain open until owner-performed motion is captured and assessed.

## Preserved snapshots

`releases/772c3e2/` retains the initial integrated public feature's deployment-specific assets, 17 studio checks, 30-second performance and five native recovery checks. `../../brand-evidence/btb-production-landing/results.json` contains 20 public landing checks and `../../brand-evidence/btb-production-catalogue/results.json` contains 39 public catalogue checks on that snapshot. `../../brand-evidence/unified-player/` contains the later separately tested landing follow-up: 24 Chromium checks, 11 playback/asset checks and WebKit at four viewports. `../../brand-evidence/btb-corrected-combined/` records the owner's four affected landing checks after integration.

Historical prototype/preview evidence remains useful for iteration history: `preview-full-suite.json`, `first-gesture-gpu.json`, `video/`, `preview-performance.json`, `preview-native-session.json` and `preview-native-recovery.json`. These are not substitutes for final public verification.

Pairing files under ignored `.mac-motion/` are private and must never be copied here. No pairing credential is retained in these records.

## Final corrected snapshot

`release-manifest.json` binds the final 058149b deployment to 20 passing public studio checks, six targeted public landing checks, 32 asset/route checks, six actual native recovery checks and the 119.63 FPS / 9.30 ms performance record. `gpu-production-response.webm` is the current deployed reversal/release/settle recording. `corrected-production-native-session.json`, its summary and `native-video/corrected-production-native.webm` contain the final real at-rest native record: 601 frame observations, zero rejected reports, p95 34.25 ms sample→frame estimate and 23.80 ms receipt→frame. Physical onset and deliberate tilt-pour remain unverified. Public landing captures and results are in `../../brand-evidence/unified-player-public/`.
