# Evidence index

All hardware entries identify this actual Mac16,8 Apple M4 Pro with macOS 26.3 and installed Chrome 153; synthetic controller fixtures are explicitly separate.

- `preview-full-suite.json`: 16 passed studio checks against the integrated built preview (5 state, 5 interaction/GPU/controller, 3 control-choice/fallback, 3 reliability/fidelity).
- `first-gesture-gpu.json` and `video/`: actual pointer → GPU response, reversal, release and decay. Recorded early prototype; final release repeated separately.
- `baseline-studio-serving.png` / `baseline-reference-comparison.png`: full legal demo transfer and reference inspection. Dynamic liquid/clear serving ice/original green pepper; demo remains labelled.
- `keyboard-variant-0.png`, `keyboard-variant-1.png`, `serving-personal-desktop.png`: distinct player fill and garnish results. No canonical result replacement.
- `preview-390x844.png`, `preview-768x1024.png`, `touch-serving-reduced-motion.png`: responsive controls and touch-emulated completion. These are not physical-phone sensor tests; phone control does not ship.
- `native-preflight-stream.ndjson.gz` / `native-preflight-summary.json`: actual native accel/gyro, 300 seconds, no phase-labelled physical action confirmation.
- `production-loopback-probe.json`: initial authenticated production HTTPS origin → actual loopback helper status, before feature deployment.
- `bridge-protocol.json`: actual running bridge authorization, Origin and single-subscriber rejection behavior.
- `release-performance.json`: 30-second headful scene frame data; inspect its `origin` to distinguish preview from final production.
- `production-native-session.json` / `native-video/` (when present): actual live sensor stream delivered to deployed scene; owner phase confirmation remains separately stated.

Broader regression evidence: `../../brand-evidence/btb-integrated-existing/results.json` has 39 existing catalogue checks; `../../brand-evidence/btb-integrated-classic/results.json` has four Classic integration checks. Final public evidence is recorded in RELEASE.md and STATUS.md. Diagnostic JSON and screenshots contain no pairing token. Local `.mac-motion/` credentials must never be copied here.
